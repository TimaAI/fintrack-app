# 🤖 Machine Learning в FinTrack

## Обзор

FinTrack использует простые алгоритмы машинного обучения для улучшения пользовательского опыта.

---

## 1. Умная категоризация

### Описание
Система автоматически предлагает категорию на основе описания транзакции, анализируя историю пользователя.

### Алгоритм

**Обучение:**
1. При сохранении транзакции извлекаются ключевые слова из описания
2. Создается связь `ключевое_слово → категория`
3. Подсчитывается частота использования

**Предсказание:**
1. Пользователь вводит описание
2. Извлекаются ключевые слова
3. Система ищет совпадения в истории
4. Возвращается наиболее частая категория

### Модель данных
```python
class CategoryPattern(models.Model):
    keyword = models.CharField(max_length=100)
    category = models.CharField(max_length=50)
    count = models.IntegerField(default=1)
```

### Пример
```
История:
- "Магнум покупки" → food (10 раз)
- "Магнум продукты" → food (5 раз)

Ввод: "Магнум"
Предсказание: food (вероятность: 100%)
```

---

## 2. Анализ паттернов трат

### Описание
Система анализирует привычки пользователя и выявляет паттерны.

### Метрики

**1. Самая затратная категория**
```python
top_category = transactions.values('category').annotate(
    total=Sum('amount')
).order_by('-total').first()
```

**2. Рост/падение расходов**
```python
change_percent = ((current_total - prev_total) / prev_total) * 100
```

**3. Средний чек по категориям**
```python
avg_expense = transactions.values('category').annotate(
    avg=Avg('amount')
)
```

---

## 3. Прогнозирование баланса

### Описание
Прогнозирует баланс на конец месяца на основе текущих трат.

### Алгоритм
```python
days_in_month = 30
days_passed = current_day
days_left = days_in_month - days_passed

daily_avg = current_expenses / days_passed
predicted_expenses = daily_avg * days_left
predicted_balance = current_balance - predicted_expenses
```

---

## 4. Рекомендации

### Типы рекомендаций

**1. Превышение бюджета**
```
"Вы потратили на 20% больше чем обычно на категорию 'Развлечения'"
```

**2. Советы по экономии**
```
"Сократив расходы на 'Транспорт' на 10%, вы сэкономите 5000₸"
```

**3. Достижения**
```
"Отличная работа! Вы потратили на 15% меньше чем в прошлом месяце"
```

---

## Будущие улучшения

### 1. NLP для категоризации
- Использование BERT или аналогов
- Обработка естественного языка
- Контекстуальное понимание

### 2. Временные ряды
- ARIMA для прогнозирования
- Сезонные паттерны
- Аномалии в тратах

### 3. Кластеризация
- Группировка похожих транзакций
- Выявление скрытых паттернов
- K-means clustering

---

**Версия:** 1.0  
**Последнее обновление:** 09.02.2026